[project]
name = "mujoco-xml-generator"
version = "0.1.0"
description = ""
authors = [
    {name = "MasashiSUZUKI", email = "masashi.lateolabrax@gmail.com"},
]
license = {text = "MIT"}
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mujoco>=3.1.3",
    "glfw>=2.7.0",
    "pillow>=10.3.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.1.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"